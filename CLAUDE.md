# 毕业设计格式检测工具

## 项目简介
Web 应用，用于检测毕业设计/论文 Word 文档的格式规范问题。
面向教师，支持多学校适配（通过规则编辑器自定义格式规范），生成可下载的检查清单报告。

## 技术栈
- 后端：Python 3 + Flask
- 文档解析：python-docx
- 前端：HTML + CSS + JS（无框架）
- 报告生成：HTML 模板

## 项目结构
```
thesis-checker/
├── app.py              # Flask 主应用
├── checker.py          # 格式检测逻辑
├── spec_parser.py      # 规范文档解析
├── report.py           # 报告生成
├── format_rules.json   # 内置格式规则
├── templates/          # Jinja2 模板
│   ├── index.html      # 首页（上传检测 + 入口按钮）
│   ├── report.html     # 检测报告模板
│   ├── examples.html   # 格式参考页面（动态适配当前规则）
│   └── editor.html     # 规则编辑器（提示词 + 粘贴 + 编辑表格 + 保存）
├── specs/              # 用户自定义规范文件（JSON）
├── static/             # 静态资源
├── uploads/            # 上传文件临时存储（gitignore）
├── reports/            # 生成的报告（gitignore）
└── CLAUDE.md           # 本文件
```

## 运行方式
```bash
cd /Users/zhong/Desktop/江西服装学院/毕设/毕业设计格式检测/thesis-checker
pip install flask python-docx
python app.py
# 访问 http://localhost:5001
```

## 格式检测规则
规则从 JSON 文件加载（`format_rules.json` 为内置规则，`specs/` 下为用户自定义规则）。
优先使用 `specs/` 下最新的规则文件，无则回退到内置规则。

### 规则编辑器（/editor）
页面打开时自动加载当前生效规则，以卡片形式展示（中文名 + 可读格式描述）。
点击卡片展开编辑表单，修改后保存为 `specs/` 下的新规范文件，内置规则 `format_rules.json` 永远不被修改。

核心流程：
1. **查看**：自动加载当前规则，每条显示为"小三号黑体，居中"等可读描述
2. **编辑**：点击卡片展开表单，修改字体/字号/加粗/对齐/行距等字段
3. **保存**：保存为 `specs/` 下的新文件，校验 section/label 必填 + 数值合法性
4. **恢复**：可随时切回内置规则

AI 批量导入（折叠区）：
1. 提供标准化提示词模板，老师复制到网页 AI（如 DeepSeek）使用
2. 老师粘贴 AI 生成的 JSON → 解析为规则卡片
3. 审核修改后保存为 `specs/` 下的规则文件

接口：
- `/editor/active` GET — 返回当前生效规则 + 来源信息
- `/editor/save` POST — 保存规则到 specs/（带校验）

### 格式参考页面（/examples）
展示正确格式示例（图片编号、脚注、参考文献），内容根据当前加载的规则动态渲染。

### 免责与隐私
- **人名与签名**：涉及学生姓名、指导教师姓名、签名等个人隐私信息的内容，完全跳过检测
- **上传文件清理**：所有上传文件在检测完成后自动删除，不做存储
- **免责声明**：首页和报告页均展示免责声明，说明检测结果仅供参考、受规则配置影响、不保证100%准确

### 不检测的部分
- **封面题目**：学生可能用不同字体设计封面，跳过字体检测
- **封面信息对齐**：学生常用 Tab/空格手动排版，段落对齐属性不可靠，跳过对齐检测
- **诚信声明书 / 独创性声明 / 使用授权声明**：模板固定内容，完全跳过
- **目录条目**：按 toc_entry 分类，不检查字体/字号/加粗
- **英文摘要使用宋体**：中国高校常见做法，跳过不报错
- **关键词段落**：「关键词：」黑体 + 关键词楷体，混合字体，跳过整体字体检测
- **参考文献/图题注中英文用 Times New Roman**：中文文档中英文内容使用 Times New Roman 是正常的
- **Segoe UI 系列字体**：当 ascii 或 eastAsia 有一个是正常字体时，用正常字体（不报错）；两个都是 Segoe UI 系列时，报告为字体错误
- **脚注与参考文献对应关系**：脚注和参考文献列表不需要一一对应

### 图片编号
- 标准格式：图X-Y（X 为章号，Y 为图序号）
- 子编号格式：图X-Y（a）、图X-Y（b）（中文括号 + 小写字母）
- 子编号从 (a) 开始连续递增，至少 2 张子图才使用子编号
- 子图不增加主序号

### 脚注
- 小五号宋体 (9pt)，单倍行距
- 编号按正文中首次引用顺序排列，从 [1] 开始连续
- 同一文献多次引用使用同一编号
- 不检查脚注与参考文献的对应关系（两者独立）

### 参考文献（GB/T 7714 顺序编码制）
- 要求不少于 20 篇，其中外文文献不少于 3 篇
- 每条须有文献类型标识：[M] 专著、[J] 期刊、[D] 学位论文、[EB/OL] 网络文献、[R] 报告
- 编号格式统一使用 [序号]
- 中文文献检查：类型标识、分隔符 "."、年份
- 英文文献检查：类型标识、年份

### 字体字号
| 部分 | 要求 |
|------|------|
| 封面信息（学生输入） | 三号宋体 (16pt)，居中 |
| 中文摘要标题"摘要" | 小三号黑体 (15pt)，居中 |
| 中文摘要正文 | 小四号楷体 (12pt) |
| 中文关键词 | 「关键词：」小四号黑体，关键词小四号楷体 |
| 英文"Abstract" | 小三号 Arial Black (15pt)，宋体可接受 |
| 英文"Key words" | 小四号 Arial Black (12pt) |
| 英文摘要正文 | 小四号 Times New Roman (12pt)，宋体可接受 |
| 目录标题"目录" | 小三号黑体 (15pt)，居中 |
| 一级标题（如 1 绪论） | 小三号宋体加粗 (15pt) |
| 二级标题（如 1.1） | 四号宋体加粗 (14pt) |
| 三级标题（如 1.1.1） | 小四号宋体加粗 (12pt) |
| 正文 | 小四号宋体 (12pt) |
| 图/表题注 | 五号宋体 (10.5pt) |
| 参考文献标题 | 小三号宋体加粗 (15pt)，居中 |
| 参考文献正文 | 五号宋体 (10.5pt) |
| 致谢标题 | 小三号宋体加粗 (15pt)，居中 |
| 致谢正文 | 小四号宋体 (12pt) |
| 附录标题 | 小三号宋体加粗 (15pt)，居中 |
| 脚注 | 小五号宋体 (9pt)，单倍行距 |
| 页眉 | 宋体小五号 (9pt)，仅正文部分有页眉；参考文献、致谢、附录不应有页眉 |

### 行间距
- 正文及标题：固定行距 22pt
- 参考文献：固定行距 18pt
- 摘要正文、关键词：固定行距 22pt

### 页面设置
- A4 纵向 (210×297mm)

### 结构顺序
封面 → 诚信声明 → 独创性声明 → 中英文摘要 → 目录 → 正文 → 参考文献 → 致谢 → 附录

### 字体检测细节
- 根据 run 内容智能选择 eastAsia / ascii：
  - run 含中文 → 用 eastAsia（中文应使用东亚字体）
  - run 纯英文/数字 → 用 ascii（西文字体）
  - ascii 为 Segoe UI 系列（emoji 字体）→ 用 eastAsia
  - eastAsia 为 Segoe UI 系列 → 用 ascii（如果 ascii 是正常西文字体）
  - 两个都是 Segoe UI 系列 → 报告
- `font=None`（未显式设置）→ 跳过不报错，Word 默认字体可接受
- 宋体变体（HYShuSongErKW、FZShuSong、SimSun 等）视为宋体
- 出错原文精确到 run 级别，标注「第X字起」，同一段内多个错误不合并
- 字号显示使用中文字号名称（如「宋体小四号」而非「12pt」）

### 标题分类
- 支持有空格（"1 绪论"）和无空格（"1绪论"）两种编号格式
- 无编号但加粗 + 字号符合标题特征的短文本（<30字）也分类为标题：
  - 15pt 加粗 → 一级标题
  - 14pt 加粗 → 二级标题
- 封面缺失内容检测：检查"指导教师""完成日期"等字段是否有值
