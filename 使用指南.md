# 毕业设计格式检测工具 — 使用指南

## 这个工具能做什么

上传学生的毕业设计 Word 文档，自动检测格式是否符合规范，包括：

- 字体字号（封面、标题、正文、摘要、参考文献等）
- 段落对齐方式
- 行间距
- 图表编号格式
- 脚注编号与格式
- 参考文献数量与格式（GB/T 7714）
- 页眉、页面设置
- 摘要与正文字数

检测结果可以下载为 HTML 报告。

---

## 第一步：确认电脑上有没有 Python

打开「终端」（Mac）或「命令提示符」（Windows），输入：

```
python3 --version
```

如果显示类似 `Python 3.x.x` 的版本号 → 你有 Python，跳到「有 Python 的老师」。

如果提示"找不到命令"或"不是内部命令" → 跳到「没有 Python 的老师」。

---

## 没有 Python 的老师

### Mac 用户

1. 打开「终端」（在启动台搜索"终端"）
2. 输入以下命令并回车：
   ```
   xcode-select --install
   ```
3. 会弹出一个安装窗口，点「安装」，等待完成（约5-10分钟）
4. 安装完成后，输入 `python3 --version` 确认显示版本号

### Windows 用户

1. 打开 Microsoft Store（开始菜单搜索"Microsoft Store"）
2. 搜索栏输入 `Python`
3. 选择 **Python 3.12**（或最新版本），点「获取/安装」
4. 安装完成后，重新打开命令提示符，输入 `python --version` 确认

---

## 有 Python 的老师

### Mac 用户

1. 解压 `江西服装学院毕设论文格式检查器.zip`
2. 双击 `start.command`
3. 首次运行会自动安装依赖（约1分钟），之后秒开
4. 浏览器会自动打开，如果没有，手动访问 `http://localhost:5001`

### Windows 用户

1. 解压 `江西服装学院毕设论文格式检查器.zip`
2. 双击 `start.bat`
3. 首次运行会自动安装依赖（约1分钟），之后秒开
4. 浏览器会自动打开，如果没有，手动访问 `http://localhost:5001`

> 注意：如果双击 .bat 文件闪退，右键选择「以管理员身份运行」。

---

## 使用方法

### 检测论文

1. 打开工具后，首页就是检测页面
2. 点击上传区域，选择学生的 Word 文件（.doc 或 .docx）
3. 可以同时选多个文件批量检测
4. 点击「开始检测」
5. 等待检测完成，查看报告
6. 点击「下载报告」可以保存为 HTML 文件发给学生

### 自定义格式规则

如果你们学校的格式要求和内置规则不同：

1. 点击首页的「规则编辑器」
2. 页面会显示当前所有格式规则
3. 点击任意规则卡片，展开编辑
4. 修改后点「确定」，再点「保存为新规范」
5. 新规范立即生效

也可以用 AI 批量导入：展开页面顶部「通过 AI 批量导入规则」，按提示操作。

### 格式参考

点击首页的「格式参考」，可以看到正确格式的示例，包括图片编号、脚注、参考文献等。内容会根据当前规则自动更新。

---

## 停止程序

关闭终端/命令提示符窗口即可。

---

## 常见问题

**Q: 双击 start.command/start.bat 没反应？**
A: Mac 用户右键选择「打开」；Windows 用户右键选择「以管理员身份运行」。

**Q: 提示"端口被占用"？**
A: 关闭之前打开的同类程序，或者重启电脑后再试。

**Q: 上传 .doc 文件检测失败？**
A: 工具会自动尝试转换 .doc 为 .docx。如果失败，请让学生另存为 .docx 格式再上传。

**Q: 想恢复默认规则怎么办？**
A: 进入「规则编辑器」，点击右上角「恢复内置规则」。

**Q: 检测结果不准确？**
A: 规则编辑器可以调整检测规则。如果发现漏检或误报，修改对应规则即可。

---

## 技术说明（可选）

- 工具基于 Python + Flask，本地运行，不上传任何文件到网络
- 格式规则存储在 `specs/` 目录下，`format_rules.json` 为内置规则
- 检测报告存储在 `reports/` 目录下，可随时删除
